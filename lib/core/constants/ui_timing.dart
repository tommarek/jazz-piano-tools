const kAutoRevealAgainDelay = Duration(milliseconds: 1500);
const kPostAgainAdvanceDelay = Duration(seconds: 1);

// Interval ear-training playback durations.
const kIntervalAudioMelodicNoteMs = 520;
const kIntervalAudioHarmonicChordMs = 760;
