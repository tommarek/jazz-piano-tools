node_modules/
# Anchored: an unanchored `build/` or `data/` also matches src/lib/data/ and any
# nested build directory, which silently drops real source from the repo.
/build/
/.svelte-kit/
/.tmp/
/data/
/test-results/
/playwright-report/
*.log
.DS_Store

# Capacitor native projects are generated by `npx cap add` and rebuilt by
# `npx cap sync`. Only the parts you hand-edit belong in git.
ios/App/App/public/
ios/App/Pods/
ios/DerivedData/
android/app/src/main/assets/public/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/
